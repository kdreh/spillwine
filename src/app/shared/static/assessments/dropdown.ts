export interface Dropdowns {
  value: number;
  name: string;
  weight: number;
  inputSource: string;
  question: string;
}
export interface dict {
  itemName: string,
  itemVals: {
    itemType: string,
    keywords: string[]
  }
}
